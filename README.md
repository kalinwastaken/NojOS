# NojOS
A simple terminal system designed to perform math and read files.
#~echo
~echo and other functions that use text can have the keywords USER, for your username, MATH, for the last referenced solution to a math problem, and DATE for the current date. This also applies to executable files and readable files (.txt).
#~execute
Executes functions in NojOS assembly, a current WIP.
